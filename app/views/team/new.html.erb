<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<%= render 'layouts/navbar' %></br>
    <%= render 'layouts/sidebar' %></br>

	<!--Custom Font-->
	<link href="https://fonts.googleapis.com/css?family=Montserrat:300,300i,400,400i,500,500i,600,600i,700,700i" rel="stylesheet">

</head>
<body>
	
		
	<div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main">
		<div class="row">
			<ol class="breadcrumb">
				<li><a href="#">
					<em class="fa fa-home"></em>
				</a></li>
				<li class="active">New Team</li>
			</ol>
		</div><!--/.row-->
		
		<div class="row">
			<div class="col-lg-12">
				<h1 class="page-header">New Projects</h1>
				<div class="panel panel-default">
						<div class="panel-heading clearfix">Please Input The Data</div>
						<div class="panel-body">
							
							<%= form_for @team, url: team_index_path, method: :post do |t| %>
						
								<div class="form-group">
									<%= t.label :Team_Name %>
									<%= t.text_field :name, class: 'form-control', required: "" %>
								</div>
								<div class="form-group">
									<%= t.hidden_field :created, value: Time.now.strftime('%Y-%m-%d'), class: 'form-control', required: "" %>
								</div>
								<div>

									<%= t.fields_for :positions do |p| %>
									<%=  p.hidden_field :user_id, value: current_user.id %>
									<%= p.hidden_field :level, value: 'contractor'  %>
									<% end %>
								</div>
								<div class="form-group">
									<%= t.submit "Create", class: 'btn btn-primary' %>
								</div>	
									
							<% end %>
						</div>
					</div>
			</div>
		</div><!--/.row-->

		<div class="row">
			
		</div>
	</div>
			
</body>
</html>
