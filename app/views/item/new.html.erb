<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<%= render 'layouts/navbar' %></br>
    <%= render 'layouts/sidebar1' %></br>

	<!--Custom Font-->
	<link href="https://fonts.googleapis.com/css?family=Montserrat:300,300i,400,400i,500,500i,600,600i,700,700i" rel="stylesheet">

</head>
<body>
	
		
	<div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main">

		
		<div class="row">
			<div class="col-lg-12">

				<div class="panel panel-default">
						<div class="panel-heading clearfix">Please Input The Data</div>
						<div class="panel-body">
							<% if @item.errors.any? %>
				  				<div class='alert alert-danger alert-dismissibele'>
				  					 <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
				  					 
				  					<% @item.errors.full_messages.each do |error| %>
				  					<span><%= error %></span></br>
				  					<% end %>
				  				</div>
				  			<% end %>
							<%= form_for @item, url: project_item_index_path, method: :post do |m| %>
						
								<div class="form-group">
								
									<%= m.hidden_field :list, class: 'form-control', required: "" %>
								</div>
								<div class="form-group">
									<%= m.label :Task %>
									<div>
									<%= m.select :task_id, @task.map { |t| [t.name, t.id, { class: t.id }] }, include_blank: 'Choose a Task', class: 'form-control' %>
								</div>
								<div class="form-group">
									<%= m.label :Milestone %>
									<div>
									
									<%= m.select :milestone_id, @milestones.map { |m| [m.name, m.id, { class: m.id }] }, include_blank: 'Choose a Milestone', class: 'form-control' %>
									
								</div>	
																
								<div class="form-group">
									<%= m.submit "Create", class: 'btn btn-primary' %>
								</div>	
									
							<% end %>
						</div>
					</div>
			</div>
		</div><!--/.row-->

		<div class="row">
			
		</div>
	</div>
			
</body>
</html>
